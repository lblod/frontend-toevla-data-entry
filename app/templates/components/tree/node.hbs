{{#if this.handlerComponent}}
  {{component this.handlerComponent node=@node experience=@experience}}
{{else}}
  {{@node.title}} ({{@node.uri}})

  <ul>
    {{#each @node.children as |child|}}
      <li>
        <LinkTo @route="experience.edit.tree-node" @model={{hash id=this.experience.id tree_node_id=@node.id}}>[E]</LinkTo>
        <Tree::Node @node={{child}} @experience={{@experience}} />
      </li>
    {{/each}}
  </ul>
{{/if}}