import criterionMatching from 'frontend-toevla-data-entry/utils/criterion-matching';
import { module, test } from 'qunit';

module('Unit | Utility | criterion-matching', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = criterionMatching();
    assert.ok(result);
  });
});
