import rockinStatechart from 'frontend-toevla-data-entry/utils/rockin-statechart';
import { module, test } from 'qunit';

module('Unit | Utility | rockin-statechart', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = rockinStatechart();
    assert.ok(result);
  });
});
