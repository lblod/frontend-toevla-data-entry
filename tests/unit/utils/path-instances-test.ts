import pathInstances from 'frontend-toevla-data-entry/utils/path-instances';
import { module, test } from 'qunit';

module('Unit | Utility | path-instances', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = pathInstances();
    assert.ok(result);
  });
});
