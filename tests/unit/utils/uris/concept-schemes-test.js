import urisConceptSchemes from 'frontend-toevla-data-entry/utils/uris/concept-schemes';
import { module, test } from 'qunit';

module('Unit | Utility | uris/concept-schemes', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = urisConceptSchemes();
    assert.ok(result);
  });
});
