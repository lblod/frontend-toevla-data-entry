import urisCriterionCodelist from 'frontend-toevla-data-entry/utils/uris/criterion-codelist';
import { module, test } from 'qunit';

module('Unit | Utility | uris/criterion-codelist', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = urisCriterionCodelist();
    assert.ok(result);
  });
});
