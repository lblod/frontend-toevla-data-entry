import customComponentMapping from 'frontend-toevla-data-entry/utils/custom-component-mapping';
import { module, test } from 'qunit';

module('Unit | Utility | custom-component-mapping', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = customComponentMapping();
    assert.ok(result);
  });
});
